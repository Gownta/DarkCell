#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main (int argc, const char ** argv) {
  //====================================
  // Usage

  if (argc < 2) {
    cerr << "Usage: " << argv[0] << " filename [filename ...]" << endl;
    return 1;
  }

  return 4;
}

